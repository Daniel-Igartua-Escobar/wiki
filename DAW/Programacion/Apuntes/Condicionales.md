# Condicionales

```java
    ?: // condición ? exp1 : exp2
    System.out.println((numero >= 0) ? "Positivo" : "Negativo");

    if (condicion) { // (anno%4 == 0 && anno%100 != 0) || anno%400 == 0
    } else if (condicion) {
    } else {
    }

    switch (mes) {
        case 1: 
            numero = "UNO";
            break;
        case 2: 
        case 3: 
        case 4: 
            numero = "OTRO";
            break;
        default:
            System.out.println();
    }
```


