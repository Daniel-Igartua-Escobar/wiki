# Math

```java
    https://docs.oracle.com/javase/7/docs/api/
    
    //Elevar a un exponente
    Math.pow(base, exponente);
    //Raiz cuadrada de un numero
    Math.sqrt();
    //Redondea a decimal
    Math.round()
    // Caasteamos a int
    (int)Math.round()

    //Generar numero aleatorio entre 10 y 1 y casteo a int
    numeroAleatorio = (int)(Math.random()* 10 + 1);
    //Generar numero aleatorio entre max y 1 y casteo a int
    (int)(Math.random() * max + 1);
     //Generar numero aleatorio entre max y min
    (int)(Math.random()*(max-min+1)+min)

    //Obtener numeros de un numero 3450 0 5 4 3
    int suerte = suma%10 + (suma/10)%10 + (suma/100)%10 + (suma/1000);
```

